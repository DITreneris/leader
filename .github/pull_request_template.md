## Summary

- 

## Changes (checklist)

- [ ] `npm run build` passes
- [ ] No changes introduce backend/login/AI API calls without approval
- [ ] Copy edits are in `en.ts` only (LT frozen — no `lt.ts` prose unless bilingual unfreeze)
- [ ] If copy/FAQ/`llms.txt` changed: four Global Context fields stay consistent in `en.ts` and `llms.txt`
- [ ] If public URL changed: README, `public/llms.txt`, and `docs/SOURCE_OF_TRUTH.md` updated
- [ ] `LEADER_PAGE_DATE_MODIFIED` bumped when FAQ or on-page SEO copy changes
- [ ] If library prompts changed: ran `npm run generate:llms-full` and committed `public/llms-full.txt`
- [ ] If deploy env touched: `npm run build && npm run verify:build` with matching `SITE_URL` / `BASE_PATH`
- [ ] Links still work (PDF, PromptAnatomy.app, promptanatomy.cloud sister CTA when touched)

Full gate: [`docs/DEFINITION_OF_DONE.md`](docs/DEFINITION_OF_DONE.md)

## Screenshots (if visual changes)

- Desktop:
- Mobile:

