---
import type { Language } from "../content/copy";
import { uiCopy } from "../content/copy";

interface Props {
  lang: Language;
}

const { lang } = Astro.props;
const flow = uiCopy[lang].flowScheme;
const [node1, node2, node3, node4] = flow.nodes;
---

<section class="glass-card relative overflow-hidden rounded-[2rem] p-6 sm:p-7" aria-label={flow.a11yLabel}>
  <div class="pointer-events-none absolute inset-x-10 top-0 h-32 rounded-full bg-brand-accent/15 blur-3xl"></div>

  <div class="relative">
    <p class="text-xs font-bold uppercase tracking-[0.28em] text-brand-accent-hover">{flow.eyebrow}</p>

    <svg
      class="mt-4 block h-auto w-full"
      viewBox="0 0 1000 520"
      preserveAspectRatio="xMidYMid meet"
      role="img"
      aria-label={flow.a11yLabel}
    >
      <title>{flow.a11yLabel}</title>
      <desc>{`${node1} → ${node2} → ${node3} → ${node4}`}</desc>

      <defs>
        <marker id="arrow" markerWidth="14" markerHeight="10" refX="12" refY="5" orient="auto" markerUnits="strokeWidth">
          <path d="M0,0 L14,5 L0,10 Z" fill="rgba(226,232,240,0.38)" />
        </marker>
        <linearGradient id="accent" x1="0" y1="0" x2="1" y2="0">
          <stop offset="0" stop-color="rgba(207,167,58,0.18)" />
          <stop offset="0.5" stop-color="rgba(232,185,60,0.55)" />
          <stop offset="1" stop-color="rgba(207,167,58,0.18)" />
        </linearGradient>
        <linearGradient id="nodeSheen" x1="0" y1="0" x2="0" y2="1">
          <stop offset="0" stop-color="rgba(255,255,255,0.09)" />
          <stop offset="0.55" stop-color="rgba(255,255,255,0.02)" />
          <stop offset="1" stop-color="rgba(255,255,255,0)" />
        </linearGradient>
        <filter id="softGlow" x="-20%" y="-20%" width="140%" height="140%">
          <feGaussianBlur stdDeviation="10" result="blur" />
          <feColorMatrix
            in="blur"
            type="matrix"
            values="1 0 0 0 0  0 0.9 0 0 0  0 0 0.55 0 0  0 0 0 0.35 0"
            result="tint"
          />
          <feMerge>
            <feMergeNode in="tint" />
            <feMergeNode in="SourceGraphic" />
          </feMerge>
        </filter>
      </defs>

      <!-- connectors: 1 -> 2 -->
      <path d="M440 130 L510 130" stroke="rgba(226,232,240,0.26)" stroke-width="3" fill="none" marker-end="url(#arrow)" />
      <!-- connector: 2 down to 3 -->
      <path d="M650 210 L650 300" stroke="rgba(226,232,240,0.18)" stroke-width="3" fill="none" marker-end="url(#arrow)" />
      <!-- connectors: 3 -> 4 -->
      <path d="M440 390 L510 390" stroke="rgba(226,232,240,0.26)" stroke-width="3" fill="none" marker-end="url(#arrow)" />

      <!-- node templates (2x2) -->
      <!-- node 1 -->
      <g>
        <rect x="60" y="70" width="380" height="120" rx="30" fill="rgba(2,6,23,0.56)" stroke="rgba(255,255,255,0.12)" stroke-width="2" />
        <rect x="62" y="72" width="376" height="56" rx="28" fill="url(#nodeSheen)" opacity="0.9" />

        <circle cx="110" cy="130" r="26" fill="rgba(255,255,255,0.06)" stroke="rgba(255,255,255,0.12)" stroke-width="2" />
        <text x="110" y="120" text-anchor="middle" dominant-baseline="middle" fill="rgba(226,232,240,0.55)" font-size="12" font-weight="800">
          1
        </text>
        <!-- icon: noise (dots) -->
        <g fill="rgba(226,232,240,0.75)">
          <circle cx="102" cy="136" r="2.2" />
          <circle cx="112" cy="136" r="2.2" />
          <circle cx="108" cy="142" r="2.2" />
          <circle cx="118" cy="142" r="2.2" />
          <circle cx="98" cy="142" r="2.2" />
        </g>

        <text x="150" y="130" fill="rgba(226,232,240,0.95)" font-size="26" font-weight="900">
          {node1}
        </text>
      </g>

      <!-- node 2 -->
      <g>
        <rect x="510" y="70" width="380" height="120" rx="30" fill="rgba(2,6,23,0.56)" stroke="rgba(255,255,255,0.12)" stroke-width="2" />
        <rect x="512" y="72" width="376" height="56" rx="28" fill="url(#nodeSheen)" opacity="0.9" />

        <circle cx="560" cy="130" r="26" fill="rgba(255,255,255,0.06)" stroke="rgba(255,255,255,0.12)" stroke-width="2" />
        <text x="560" y="120" text-anchor="middle" dominant-baseline="middle" fill="rgba(226,232,240,0.55)" font-size="12" font-weight="800">
          2
        </text>
        <!-- icon: prompt (brackets) -->
        <path
          d="M552 145c-6 0-10-4-10-10v-10c0-6 4-10 10-10"
          fill="none"
          stroke="rgba(226,232,240,0.75)"
          stroke-width="3"
          stroke-linecap="round"
        />
        <path
          d="M568 105c6 0 10 4 10 10v10c0 6-4 10-10 10"
          fill="none"
          stroke="rgba(226,232,240,0.75)"
          stroke-width="3"
          stroke-linecap="round"
        />

        <text x="600" y="130" fill="rgba(226,232,240,0.95)" font-size="26" font-weight="900">
          {node2}
        </text>
      </g>

      <!-- node 3 (accent) -->
      <g filter="url(#softGlow)">
        <rect x="60" y="330" width="380" height="120" rx="30" fill="rgba(2,6,23,0.6)" stroke="url(#accent)" stroke-width="3" />
        <rect x="60" y="330" width="380" height="120" rx="30" fill="none" stroke="rgba(207,167,58,0.22)" stroke-width="10" />
        <rect x="62" y="332" width="376" height="56" rx="28" fill="url(#nodeSheen)" opacity="0.95" />

        <circle cx="110" cy="390" r="26" fill="rgba(207,167,58,0.14)" stroke="rgba(232,185,60,0.35)" stroke-width="2" />
        <text x="110" y="380" text-anchor="middle" dominant-baseline="middle" fill="rgba(255,236,179,0.75)" font-size="12" font-weight="800">
          3
        </text>
        <!-- icon: output (check) -->
        <path
          d="M98 394l8 8 18-18"
          fill="none"
          stroke="rgba(255,236,179,0.9)"
          stroke-width="4"
          stroke-linecap="round"
          stroke-linejoin="round"
        />

        <text x="150" y="384" fill="rgba(255,236,179,0.98)" font-size="22" font-weight="950">
          {node3}
        </text>
      </g>

      <!-- node 4 -->
      <g>
        <rect x="510" y="330" width="380" height="120" rx="30" fill="rgba(2,6,23,0.56)" stroke="rgba(255,255,255,0.12)" stroke-width="2" />
        <rect x="512" y="332" width="376" height="56" rx="28" fill="url(#nodeSheen)" opacity="0.9" />

        <circle cx="560" cy="390" r="26" fill="rgba(255,255,255,0.06)" stroke="rgba(255,255,255,0.12)" stroke-width="2" />
        <text x="560" y="380" text-anchor="middle" dominant-baseline="middle" fill="rgba(226,232,240,0.55)" font-size="12" font-weight="800">
          4
        </text>
        <!-- icon: safety (shield) -->
        <path
          d="M560 401c10-5 16-10 16-20v-12l-16-7-16 7v12c0 10 6 15 16 20z"
          fill="none"
          stroke="rgba(226,232,240,0.75)"
          stroke-width="3"
          stroke-linejoin="round"
        />

        <text x="600" y="390" fill="rgba(226,232,240,0.95)" font-size="26" font-weight="900">
          {node4}
        </text>
      </g>
    </svg>
  </div>
</section>

