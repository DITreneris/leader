---
description: Visual, copy, and conversion standards for the CEO landing page
globs: src/**/*.{astro,ts,css}
alwaysApply: false
---

# Visual And Copy Standards

Full token tables, `src/components/ds/` primitives, section templates, and anti-patterns: **`docs/DESIGN_SYSTEM.md`**.

- Use executive language: short, concrete, outcome-driven. Avoid beginner AI education language.
- Prioritize clarity over decoration. Gradients, glass cards, memes, and motion must support the message.
- Keep CTAs focused: run the **check** (`#demo` / clarity scenarios) after `#context`, then go to `#kit` and the full PromptAnatomy system.
- New outbound links to PromptAnatomy, Telegram, or tracked URLs: build with `src/constants/outboundLinks.ts` (not raw strings) and extend [`docs/UTM_MATRIX.md`](../../docs/UTM_MATRIX.md) when `utm_*` values change—see **`docs/DESIGN_SYSTEM.md`** banner hierarchy. **Paste-to-assistant** (ChatGPT / Claude / Gemini) links use `buildConsumerAiUrl` on the same module—never hand-roll query strings. (Hero and modules framing use **“2-minute”** where copy says so—stay consistent with locales, not mixed “2 vs 3 min.”)
- Use memes as visual breaks only; do not let them dominate the page.
- For every new visual section, check mobile order: headline, value, CTA or useful visual.
- Keep the color system premium and restrained: dark navy base (#0B1320), white text, gold accent (#CFA73A with #E8B93C hover) and the gold CTA gradient (#FFCC33 to #FFB300) matching the PromptAnatomy.app mother brand. Do not reintroduce cyan/blue as the primary accent. Status colors stay functional: rose for risk, emerald for action, amber for warning.
- When adding images, prefer SVG for diagrams and AVIF/WebP for screenshots; define dimensions or aspect ratio.
